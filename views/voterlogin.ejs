<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Voter Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="grid grid-cols-6">
      <div class="col-span-2 col-start-3">
        <div class="my-4">
          <a href="/" class="text-2xl font-semibold text-gray-600"
            >My Online Voting App</a
          >
        </div>

        <h6 class="mb-4 mt-4 font-semibold text-center text-xl text-gray-900">
          Voter Log-In
        </h6>

        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <li class="mb-2 text-rose-500"> <%= messages.error[i] %></li>
          <% } %>
        <% } %>

        <form action="/sessionVoter/<%= election.url %>" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div>
            <label for="voterID" class="py-1 text-xl text-gray-900"
              >Voter ID :</label
            >
            <input
              type="text"
              id="voterID"
              name="voterID"
              required
              class="w-full border border-gray-600 rounded px-5 py-2 my-2 text-gray-900"
            />
          </div>
          <div>
            <label for="pwd" class="py-1 text-xl text-gray-900"
              >Password :</label
            >
            <input
              type="password"
              id="pwd"
              name="password"
              required
              class="w-full border border-gray-600 rounded px-5 py-2 my-2 text-gray-900"
            />
          </div>
          <div>
            <input
              type="submit"
              value="Sign in"
              class="bg-green-600 text-white px-5 py-2 rounded my-1"
            />
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
