<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="grid grid-cols-6">
      <div class="col-span-2 col-start-3">
        <div class="my-4">
          <a href="/" class="text-2xl font-semibold text-gray-600"
            >My Online Voting App</a
          >
        </div>

        <h6 class="mb-2 text-xl mt-4 text-gray-900">Sign Up as a new admin</h6>

        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <li class="mb-2 text-rose-500"> <%= messages.error[i] %></li>
          <% } %>
        <% } %>

        <form action="/users" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div>
            <label for="fname" class="py-1 text-xl text-gray-900"
              >FirstName :</label
            >
            <input
              type="text"
              id="fname"
              name="firstName"
              class="w-full border border-gray-600 rounded px-5 py-2 my-2 text-gray-900"
            />
          </div>
          <div>
            <label for="lname" class="py-1 text-xl text-gray-900"
              >Last Name :</label
            >
            <input
              type="text"
              id="lname"
              name="lastName"
              class="w-full border border-gray-600 rounded px-5 py-2 my-2 text-gray-900"
            />
          </div>
          <div>
            <label for="emailID" class="py-1 text-xl text-gray-900"
              >Email :</label
            >
            <input
              type="email"
              id="emailID"
              name="email"
              class="w-full border border-gray-600 rounded px-5 py-2 my-2 text-gray-900"
            />
          </div>
          <div>
            <label for="pwd" class="py-1 text-xl text-gray-900"
              >Password :</label
            >
            <input
              type="password"
              id="pwd"
              name="password"
              class="w-full border border-gray-600 rounded px-5 py-2 my-2 text-gray-900"
            />
          </div>
          <div>
            <input
              type="submit"
              value="Sign Up"
              class="bg-green-600 text-white px-5 py-2 rounded my-1"
            />
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
